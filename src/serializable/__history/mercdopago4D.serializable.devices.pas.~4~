unit mercdopago4D.serializable.devices;

interface

uses
  GBJSON.Attributes;

type
  TDevicesSerialize = class
  private
    [JSONProp('id')]
    FId: String;
    [JSONProp('operating_mode')]
    FOperatingMode: String;
  public
    function Id(Value: String): TDevicesSerialize; overload;
    function Id: String; overload;
    function OperatingMode(Value: String): TDevicesSerialize; overload;
    function OperatingMode: String; overload;
//    property Id: String read FId write FId;
//
//    property OperatingMode: String read FOperatingMode write FOperatingMode;

    class function New: TDevicesSerialize;
  end;

implementation

function TDevicesSerialize.Id(Value: String): TDevicesSerialize;
begin
  Result := Self;
  FId := Value;
end;

function TDevicesSerialize.Id: String;
begin
  Result := FId;
end;

class function TDevicesSerialize.New: TDevicesSerialize;
begin
  Result := Self.Create;
end;

function TDevicesSerialize.OperatingMode(Value: String): TDevicesSerialize;
begin

end;

function TDevicesSerialize.OperatingMode: String;
begin

end;

end.
