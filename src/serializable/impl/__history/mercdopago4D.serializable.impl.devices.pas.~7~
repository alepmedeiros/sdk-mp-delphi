unit mercdopago4D.serializable.impl.devices;

interface

uses
  mercdopago4D.serializable.interfaces;

type
  TDevices = class(TInterfacedObject, iDevices)
  private

    constructor Create;
    destructor Destroy; override;
  public
    class function New: iDevices;

    function Id(Value: String): iDevices; overload;
    function Id: String; overload;
    function OperatingMode(Value: String): iDevices; overload;
    function OperatingMode: String; overload;
  end;

implementation


constructor TDevices.Create;
begin

end;

destructor TDevices.Destroy;
begin

  inherited;
end;

function TDevices.Id(Value: String): iDevices;
begin
  Result := Self;
end;

function TDevices.Id: String;
begin

end;

class function TDevices.New: iDevices;
begin
  Result := Self.Create;
end;

function TDevices.OperatingMode(Value: String): iDevices;
begin
  Result := Self;
end;

function TDevices.OperatingMode: String;
begin

end;

end.
